# Metrics for VCR Q→A training and evaluation.
# Q→A accuracy: fraction of questions with correct top-1 answer candidate.

device: "auto"

train:
  - _target_: src.metrics.VCRQAAccuracy
    name: "VCR_QA_Accuracy"

inference:
  - _target_: src.metrics.VCRQAAccuracy
    name: "VCR_QA_Accuracy"
