# Default inference entrypoint config.
#
# Uses the lightweight GQA demo path so `python inference.py` has a valid
# default config. Task-specific evaluation should still use explicit
# `--config-name inference_gqa|inference_vcr`.

defaults:
  - inference_gqa
